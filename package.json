{
  "name": "us-bme-test",
  "version": "1.0.0",
  "private": true,
  "description": "EOG Practice Portal — kid-friendly NC EOG-style Reading & Math test simulator on Cloudflare",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "validate": "node scripts/validate.mjs",
    "seed:local": "node scripts/seed.mjs",
    "seed:remote": "node scripts/seed.mjs --remote"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
